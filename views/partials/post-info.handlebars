<div class="container-fluid">
    <div class="card" style="width:fit-content">
        <div>
            <div>
                <a href="/post/{{id}}" class="badge badge-primary">{{title}}</a>
            </div>
            <div>
                <p id="comment-subtitle">
                    Posted by <b>{{user.username}}</b> 
                </p>
            </div>
        </div>
        <div class="content-section">
            <p>{{content}}</p>
        </div>
    </div>
</div>

